'use strict';
/* =============================================================================
   تم — خدمة المركبات (علم، بالتعاون مع الإدارة العامة للمرور)
   ---------------------------------------------------------------------------
   ليست منتجاً تجارياً بواجهة برمجية عامة كزوهو: الربط يمر بقناة رسمية مع
   "علم"، وبياناته تُسلَّم في وثيقة ربط خاصة بكل منشأة.

   لذلك لا نخترع هنا نقاط اتصال ولا أسماء حقول من عندنا — ذلك يعطي شاشة
   تبدو جاهزة ثم تفشل عند أول استعمال. الحقول أدناه هي ما تطلبه وثائق الربط
   الحكومية عادةً، وتُثبَّت نهائياً حين تصل وثيقة المنشأة.

   الشركة كانت مربوطة بتم في نظامها القديم، فالاتفاقية قائمة والمطلوب
   استخراج بياناتها من مزوّد النظام السابق لا طلب ربط جديد.
   ============================================================================= */

const LABEL = 'تم';

const FIELDS = [
  { key: 'base_url', label: 'عنوان الخدمة', type: 'text', required: true,
    hint: 'من وثيقة الربط — يبدأ بـ https://' },

  { key: 'facility_no', label: 'رقم المنشأة', type: 'text', required: true,
    hint: 'رقم منشأة الشركة المسجَّل لدى الخدمة' },

  { key: 'client_id', label: 'معرّف العميل', type: 'text', required: true },

  { key: 'client_secret', label: 'المفتاح السرّي', type: 'secret', required: true,
    hint: 'لا يظهر بعد الحفظ' },
];

// حتى تصل وثيقة الربط لا يوجد ما نناديه — والصراحة أنفع من شاشة تُوهم بالجاهزية
const PENDING_DOCS =
  'الحقول محفوظة، لكن الربط لا يعمل بعد: تم خدمة حكومية بلا واجهة برمجية عامة، ' +
  'ويلزم عنوان الخدمة وطريقة المصادقة من وثيقة الربط الخاصة بالمنشأة. ' +
  'اطلبها من مزوّد النظام القديم — الشركة كانت مربوطة به فالاتفاقية قائمة.';

async function test() {
  throw new Error(PENDING_DOCS);
}

module.exports = { name: 'tam', LABEL, FIELDS, test, pending: true, PENDING_DOCS };
